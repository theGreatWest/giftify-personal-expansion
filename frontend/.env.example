# Frontend (Vite) example env
# Copy to .env, .env.local, or .env.production as needed

# Where the frontend is served (for client-side links)
VITE_FRONT_BASE_URL=http://localhost:5173

# Backend API endpoint base URL
VITE_API_BASE_URL=http://localhost:8080

# Optional label
VITE_ENV=local
